use std::io::{self, Write};
use std::thread::sleep;
use std::time::Duration;

fn main() {
    println!("🎯 Reto 01: Loader de Tres Puntos\n");
    
    // TODO: Implementa el loader aquí
    // Pistas:
    // 1. Crea un loop infinito
    // 2. Usa un contador para alternar entre 1, 2 y 3 puntos
    // 3. Limpia la pantalla en cada iteración
    // 4. Imprime "Cargando" + los puntos
    // 5. Haz flush del stdout
    // 6. Duerme por 300ms
    
    loop {
        // Tu código aquí
        
        sleep(Duration::from_millis(300));
    }
}